import { StyleSheet } from 'react-native';

const inputStyles = StyleSheet.create({
    textInput: {
        borderWidth: 2,
        borderColor: '#fff',
        margin: 10,
        padding: 10,
        color: '#fff',
        backgroundColor: '#333',
        borderRadius: 5,
    },
});

export default inputStyles;
