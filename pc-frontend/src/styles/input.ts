import { StyleSheet } from 'react-native';

const inputStyles = StyleSheet.create({
    textInput: {
        color: '#fff',
        backgroundColor: '#333',
        borderRadius: 4,
        textAlign: 'center',
    },
});

export default inputStyles;
